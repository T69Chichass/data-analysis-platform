services:
  - type: web
    name: insurance-policy-analyzer
    env: python
    buildCommand: echo "No dependencies needed - using Python standard library"
    startCommand: python app.py
    plan: free
